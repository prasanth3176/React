import React, {useReducer} from "react";
import Counter from "./useReducerData";

const UseReducer = () => {
  return (
    <div>
        <h1>Counter using useReducer</h1>
      <Counter />
    </div>
  );
};

export default UseReducer;
